# from django.apps import AppConfig

# class AiteamConfig(AppConfig):
#     default_auto_field = "django.db.models.BigAutoField"
#     name = "Server_Config.Server_Side"  # here django searche the folder that contains the Server side of the app
#     # if the path is wrong tha cause the issue: ModuleNotFoundError: No module named 'Server_Config'

